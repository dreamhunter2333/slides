{
  "name": "slides",
  "private": true,
  "type": "module",
  "scripts": {
    "build:all": "rm -rf dist && pnpm -r --filter !home build && cd home && pnpm build",
    "dev:all": "pnpm -r dev",
    "export:all": "pnpm -r export"
  },
  "devDependencies": {
    "@slidev/cli": "^51.3.0",
    "@slidev/theme-default": "^0.25.0",
    "@slidev/theme-seriph": "^0.25.0",
    "http-server": "^14.1.1",
    "playwright-chromium": "^1.51.0"
  }
}
