{
  "name": "slides",
  "private": true,
  "type": "module",
  "scripts": {
    "build:all": "pnpm -r build && node index.js",
    "dev:all": "pnpm -r dev",
    "export:all": "pnpm -r export"
  },
  "devDependencies": {
    "@slidev/cli": "^0.49.16",
    "@slidev/theme-default": "^0.25.0",
    "@slidev/theme-seriph": "^0.25.0",
    "http-server": "^14.1.1",
    "playwright-chromium": "^1.45.1"
  }
}
